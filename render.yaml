services:
  - type: web
    name: yt-dlp-api
    runtime: node
    repo: https://github.com/ayanmirza99/downloader-backend
    plan: free
    buildCommand: "npm install"
    startCommand: "node server.js"
    envVars:
      - key: NODE_ENV
        value: production
